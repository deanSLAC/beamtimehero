{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "Intentionally empty. Runtime permissions for the embedded BeamtimeHero agent live in ./agent.settings.json and are loaded by server/claude_cli_backend.py via `--settings`. Per-developer overrides belong in ~/.claude/settings.json."
}
